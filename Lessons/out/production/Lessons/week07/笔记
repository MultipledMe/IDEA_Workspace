static 创建公共区域，用于对象存放数据，其他类也可以通过类名.静态方法名使用
类及成员修饰符
public:修饰类名，该类可以被其他类访问或引用。
            修饰成员变量或方法，表示该类的成员不仅可以被内部成员访问，而且可以被其它类的成员访问
private:限定的成员变量只能在类内部使用
protected:用于修饰类成员，可以被包（包括其他包）内所有类及其子类访问
default:缺省修饰符，用于同一包同一类，默认省略
Java只支持单继承，即只有一个父类，子类可以继承除private修饰外的成员变量和方法
Java里的多继承依靠接口interface
重写：子类新增的成员变量和方法与父类对应的同名，称为覆盖或重写Override
Java子父类的类型转换。子转父，显或隐皆可；父转子，只有显；
