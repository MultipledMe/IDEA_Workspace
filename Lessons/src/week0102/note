1.顺序存储：地址连续，一次存放，起始地址>基地址
2.LOC(a[i]) = LOC(a[0]) + i*C
3.顺序存储：
逻辑相邻，存储相邻
存储密度高：数据元素的值所需的存储空间/该元素实际所需的存储空间
便于随机存储
不便于插入、删除操作
4.抛出异常时要注意接口中定义的方法要声明
5.单链表：一组地址任意的存储单元存放线性表中的数据元素
  Node = element + pointer
  结点序列表示线性表称为链表
  头指针：第一个数据元素的存储地址
  第一个节点之前有时会加头节点
  线性表为空表，头结点的指针，即头指针为空指针，尾结点由于无后驱，指针为空。
  